export enum GraphComparatorOptions {
    NOT = 'not',
    GT = 'gt',
    LT = 'lt',
    GTE = 'gte',
    LTE = 'lte',
    IN = 'in',
    NOT_IN = 'not_in'
}

export enum GraphFiltersDataType {
    Input = 'Input',
    MultiAutocomplete = 'MultiAutocomplete',
    MultiButtonSelection = 'MultiButtonSelection',
    RangeSlider = 'RangeSlider'
}

export enum GraphFiltersHelperType {
    Price = 'Price'
}
