import { makeStyles } from '@mui/styles';

const styles = makeStyles(() => ({
    container: {
        padding: 24
    },
    alert: {
        display: 'flex',
        justifyContent: 'center'
    }
}));

export default styles;
