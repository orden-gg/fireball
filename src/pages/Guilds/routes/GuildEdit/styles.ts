import { createStyles, makeStyles } from '@mui/styles';

export const guildEditStyles = makeStyles(() =>
  createStyles({
    guildEditError: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }
  })
);
