import React, { } from 'react';

export default function GuildsDetails({ guild, gotchis }) {
    // const classes = styles();

    return (
        <>
            {/* Guild Details */}
        </>
    );
}
