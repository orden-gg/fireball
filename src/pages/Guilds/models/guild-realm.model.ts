export interface GuildRealm {
  id: string;
  parcelId: string;
  coordinateX: string;
  coordinateY: string;
  size: string;
  district: string;
  parcelHash: string;
  owner: {
    id: string;
  };
}
