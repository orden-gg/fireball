import { makeStyles } from "@mui/styles";

const styles = makeStyles( theme => ({
    citadel: {
        position: 'relative'
    },

    parsel: {
        position: 'absolute',
        left: 0,
        bottom: 0
    }

}));

export default styles;