import { makeStyles } from '@mui/styles';

const styles = makeStyles(() => ({
    filtersWrapper: {
        display: 'flex',
        flexDirection: 'column',
        padding: 12
    }
}));

export default styles;
