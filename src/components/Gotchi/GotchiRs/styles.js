import { makeStyles } from '@mui/styles';

const styles = makeStyles(() => ({
    gotchiRsWrapper: {
        fontSize: 13,
        margin: '0 2px'
    },
    modifiedRs: {
        fontWeight: 600
    }
}));

export default styles;
