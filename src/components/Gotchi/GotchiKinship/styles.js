import { makeStyles } from '@mui/styles';

const styles = makeStyles(() => ({
    gotchiKinship: {
        fontSize: 13,
        display: 'flex',
        alignItems: 'center',
        fontWeight: 500
    },
    gotchiKinshipIcon: {
        width: 12,
        marginRight: 2
    }
}));

export default styles;
