import { createStyles, makeStyles } from '@mui/styles';

export const styles = makeStyles(() => createStyles({
    gotchiKinship: {
        fontSize: 13,
        display: 'flex',
        alignItems: 'center',
        fontWeight: 500
    },
    gotchiKinshipIcon: {
        width: 12,
        marginRight: 2
    }
}));
